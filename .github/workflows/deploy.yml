name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      # 1️⃣ Checkout repository
      - uses: actions/checkout@v3

      # 2️⃣ Set up Python environment
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.11

      # 3️⃣ Install dependencies
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install flake8 pytest

      # 4️⃣ Run code linting (PEP8)
      - name: Lint code
        run: flake8 .

      # 5️⃣ Run tests (skip if none exist)
      - name: Run tests
        run: pytest || echo "No tests configured, skipping."

      # 6️⃣ Build Docker image
      - name: Build Docker image
        run: docker build -t spacex-telemetry-dashboard .

      # 7️⃣ Test Docker container
      - name: Test Docker container
        run: |
          docker run --rm -d -p 5000:5000 spacex-telemetry-dashboard
          sleep 5
          curl -f http://localhost:5000 || exit 1

  # Optional: Deploy to Render (uncomment to activate)
  # deploy:
  #   runs-on: ubuntu-latest
  #   needs: build-and-test
  #   steps:
  #     - uses: actions/checkout@v3
  #     - name: Deploy to Render
  #       uses: render-examples/deploy-flask@v1
  #       with:
  #         api_key: ${{ secrets.RENDER_API_KEY }}
  #         service_id: ${{ secrets.RENDER_SERVICE_ID }}
